<!--
============================================
Title: Exercise 5.2 - EJS Templates
Author: Professor Krasso
Date 01 February 2022
Modified By: Orawan Rabampho
Description: Create EJS Templates.
Bootstrap: https://www.w3schools.com/whatis/tryit.asp?filename=trywhatis_bootstrap&stacked=h
===========================================
-->
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.1/dist/css/bootstrap.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.1/dist/js/bootstrap.bundle.min.js"></script>
    <title>Exercise 3.3 - Advanced-routing</title>
</head>
    <body>
    <nav class="navbar navbar-expand-sm bg-dark navbar-dark">
        <ul class="navbar-nav">
            <li class="nav-item active">
                <a class="nav-link" href="#">Home</a>
            </li>
            <li class="nav-item active">
                <a class="nav-link" href="#">About</a>
            </li>
            <li class="nav-item active">
                <a li class="nav-link" href="#">Contact Us</a>
            </li>
        </ul>
    </nav>
<br/><br/>
        <div role="main" class="container">
        <h2>Animals List</h2>
        <% for (var k = 0; k <animals.length; k++){ %>
        
            <!--if-->
        <% if (animals[k]== "Fish"){%>
            <div class="alert alert-success" role="alert">
                <strong><%= animals[k]%></strong></div>
        <% } %>
        <!--//end if-->
       
        <!--if-->
        <% if (animals[k]== "Dog"){ %>
        <div class="alert alert-info" role="alert">
            <strong><%= animals[k]%></strong></div>
        <% } %>
        <!--//end if-->

         <!-- if -->
        <% if (animals[k] == "Cat") { %>
        <div class="alert alert-warning" role="alert">
          <strong><%= animals[k] %></strong>
        </div>
        <% } %>
        <!-- // end if -->

        <!-- if -->
        <% if (animals[k] == "Bird") { %>
        <div class="alert alert-danger" role="alert">
          <strong><%= animals[k] %></strong>
        </div>
        <% } %>
        <!-- // end if -->

        <% } %>
        <!--end for loop-->
        </div>
    </body>
</html>    